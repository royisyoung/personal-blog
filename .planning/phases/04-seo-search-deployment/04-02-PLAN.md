---
phase: 04-seo-search-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - .gitignore
  - lib/search.ts
autonomous: true
requirements: [SRCH-01]
user_setup: []

must_haves:
  truths:
    - 'Pagefind 依赖已安装'
    - 'Pagefind 在构建后索引静态输出'
    - 'Pagefind 搜索索引生成到 out/pagefind 目录'
    - 'React hook 暴露搜索功能给 UI 组件'
    - 'Pagefind 输出不被文件追踪包含在构建中'
  artifacts:
    - path: 'package.json'
      provides: 'pagefind 依赖和构建后索引脚本'
      contains: 'pagefind'
    - path: 'lib/search.ts'
      provides: 'usePagefindSearch React hook'
      exports: ['usePagefindSearch']
    - path: 'next.config.ts'
      provides: 'Pagefind 输出排除配置'
      contains: 'pagefind'
  key_links:
    - from: 'package.json build 脚本'
      to: 'pagefind --source out'
      via: 'post-next-build 执行'
      pattern: 'pagefind --source'
    - from: 'lib/search.ts'
      to: 'window.pagefind'
      via: 'global script loading'
      pattern: 'window.pagefind'
    - from: 'next.config.ts'
      to: 'pagefind output exclusion'
      via: 'outputFileTracingExcludes'
      pattern: 'pagefind'
---

<objective>
集成 Pagefind 静态全文搜索，在构建时生成搜索索引，并提供 React Hook 供 UI 使用

Purpose: 实现零后端全文搜索，用户可以快速搜索博客内容。Pagefind 在构建时预生成索引，客户端搜索不需要服务器。Output: 安装 pagefind 依赖，更新构建脚本，配置输出排除，创建搜索 React hook。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-seo-search-deployment/04-CONTEXT.md
@.planning/phases/04-seo-search-deployment/04-RESEARCH.md

<interfaces>
From 04-RESEARCH.md pattern:
```typescript
export function usePagefindSearch(query: string): any[];
// Returns search results array
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Pagefind dependency</name>
  <files>/Users/xuyang/code/my-claudes/package.json</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/package.json
  </read_first>
  <action>
    1. 运行 `npm install pagefind --save` 安装 Pagefind
    2. 修改 package.json build 脚本，在 `next build` 之后、sitemap 生成之前添加 Pagefind 索引：
       - New build script: `"build": "next build --turbopack && npx pagefind --source out && node scripts/generate-sitemap.mjs"`
  </action>
  <acceptance_criteria>
    - `grep -A 1 "pagefind" /Users/xuyang/code/my-claudes/package.json` 找到了 pagefind 依赖
    - `grep -q "pagefind --source out" /Users/xuyang/code/my-claudes/package.json` 确认构建脚本包含 Pagefind
    - npx pagefind --version 可执行
  </acceptance_criteria>
  <verify>
    <automated>grep -n "pagefind" /Users/xuyang/code/my-claudes/package.json</automated>
  </verify>
  <done>pagefind 已安装，构建脚本已更新</done>
</task>

<task type="auto">
  <name>Task 2: Configure Next.js for Pagefind</name>
  <files>/Users/xuyang/code/my-claudes/next.config.ts /Users/xuyang/code/my-claudes/.gitignore</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/next.config.ts
    - /Users/xuyang/code/my-claudes/.gitignore
  </read_first>
  <action>
    1. Update `outputFileTracingExcludes` in next.config.ts to exclude pagefind directory:
       - Add `'**': ['pagefind/**']` already in the config? If not present, add it.
       - This prevents Next.js from tracing pagefind binary into output
    2. Add `out/pagefind` to `.gitignore` since index is generated at build time
  </action>
  <acceptance_criteria>
    - `grep -q "pagefind" /Users/xuyang/code/my-claudes/next.config.ts`
    - `grep -q "out/pagefind" /Users/xuyang/code/my-claudes/.gitignore`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "pagefind" /Users/xuyang/code/my-claudes/next.config.ts && grep -n "out/pagefind" /Users/xuyang/code/my-claudes/.gitignore</automated>
  </verify>
  <done>Next.js 已配置排除 pagefind，gitignore 已更新</done>
</task>

<task type="auto">
  <name>Task 3: Create usePagefindSearch React hook</name>
  <files>/Users/xuyang/code/my-claudes/lib/search.ts</files>
  <read_first>
    - (new file) /Users/xuyang/code/my-claudes/lib/search.ts
  </read_first>
  <action>
    1. Create `lib/search.ts`
    2. Implement `usePagefindSearch` hook that:
       - Uses useEffect to load Pagefind index from `window.pagefind`
       - Stores index reference in state
       - When query changes (length ≥ 2), performs search
       - Returns array of search results
       - Handles empty query and loading state
    3. Define TypeScript types for Pagefind index and results
    4. Follow the pattern from 04-RESEARCH.md (code example verified)
  </action>
  <acceptance_criteria>
    - File `lib/search.ts` exists
    - File exports `usePagefindSearch` function
    - File contains `window.pagefind` global type declaration
    - File uses React hooks (useState, useEffect)
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/xuyang/code/my-claudes/lib/search.ts && grep -n "export function usePagefindSearch" /Users/xuyang/code/my-claudes/lib/search.ts</automated>
  </verify>
  <done>搜索 hook 已创建，可被搜索 UI 组件使用</done>
</task>

</tasks>

<verification>
- 运行完整构建 `npm run build` 确认 Pagefind 索引生成成功
- 检查 `out/pagefind/` 目录包含索引文件
</verification>

<success_criteria>

- Pagefind 依赖已安装到 package.json
- 构建脚本在 next build 之后运行 pagefind
- Next.js 配置排除 pagefind 输出
- usePagefindSearch hook 存在并导出正确
- .gitignore 排除了生成的搜索索引
- npx pagefind --source out 成功生成索引 </success_criteria>

<output>
After completion, create `.planning/phases/04-seo-search-deployment/04-02-SUMMARY.md`
</output>
