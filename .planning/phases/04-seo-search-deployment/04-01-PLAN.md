---
phase: 04-seo-search-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/about/page.tsx
  - app/posts/[slug]/page.tsx
  - app/categories/[category]/page.tsx
  - app/tags/[tag]/page.tsx
  - app/api/og/route.tsx
  - scripts/generate-sitemap.mjs
  - public/robots.txt
  - .env.example
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, DEP-01]
user_setup: []

must_haves:
  truths:
    - '每个页面都有正确的 title、description 元标签'
    - '每个文章都有动态生成的 OG 图片用于社交分享'
    - 'sitemap.xml 在构建时自动生成，包含所有页面和文章'
    - 'robots.txt 存在且包含 sitemap 引用'
    - '项目可以在 Vercel 上一键构建部署'
  artifacts:
    - path: 'app/api/og/route.tsx'
      provides: '动态 OG 图片生成端点'
      exports: ['GET']
      min_lines: 30
    - path: 'scripts/generate-sitemap.mjs'
      provides: '构建时生成 sitemap 脚本'
      contains: 'getAllPosts'
    - path: 'public/robots.txt'
      provides: '搜索引擎抓取指令'
      contains: 'Sitemap:'
    - path: 'package.json'
      provides: '添加 @vercel/og 依赖和构建后脚本'
      contains: '@vercel/og'
  key_links:
    - from: '所有 page.tsx 文件'
      to: 'Next.js generateMetadata'
      via: 'generateMetadata async export'
      pattern: 'export async function generateMetadata'
    - from: 'posts/[slug]/page.tsx'
      to: '/api/og'
      via: 'OG 图片 URL 构造'
      pattern: 'api/og?title='
    - from: 'package.json build 脚本'
      to: 'scripts/generate-sitemap.mjs'
      via: 'post-build 执行'
      pattern: 'generate-sitemap'
---

<objective>
实现 SEO 基础设施：元标签、动态 OG 图片、sitemap 和 robots.txt

Purpose: 让博客能够被搜索引擎正确索引，社交分享时显示美观的预览，并且满足 Vercel 部署配置要求。Output: 添加 @vercel/og 依赖，创建 OG 图片端点，添加 sitemap 生成脚本，创建 robots.txt，更新所有页面的元数据。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-seo-search-deployment/04-CONTEXT.md
@.planning/phases/04-seo-search-deployment/04-RESEARCH.md
@.planning/phases/04-seo-search-deployment/04-UI-SPEC.md

<interfaces>
From lib/content.ts:
```typescript
export function getAllPosts(): Post[];
export function getPostBySlug(slug: string): Post | undefined;
```

From app/layout.tsx (existing root metadata):

```typescript
export const metadata: Metadata = {
  title: 'MyClaudes - Personal Technology Blog',
  description: 'A clean, fast static technology blog',
};
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and update build script</name>
  <files>/Users/xuyang/code/my-claudes/package.json</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/package.json
  </read_first>
  <action>
    1. 运行 `npm install @vercel/og --save` 安装动态 OG 图片生成依赖
    2. 修改 package.json 中的 build 脚本，在构建完成后执行 sitemap 生成脚本：
       - Original: `"build": "next build --turbopack"`
       - Modified: `"build": "next build --turbopack && node scripts/generate-sitemap.mjs"`
  </action>
  <acceptance_criteria>
    - `grep -A 1 "@vercel/og" /Users/xuyang/code/my-claudes/package.json` 找到了 @vercel/og 依赖
    - `grep -q "generate-sitemap" /Users/xuyang/code/my-claudes/package.json` 确认构建脚本包含 sitemap 生成
    - `node --version` 能够执行 Node.js 脚本
  </acceptance_criteria>
  <verify>
    <automated>grep -n "@vercel/og" /Users/xuyang/code/my-claudes/package.json && grep -n "generate-sitemap" /Users/xuyang/code/my-claudes/package.json</automated>
  </verify>
  <done>@vercel/og 依赖已安装，构建脚本已更新包含 sitemap 生成</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic OG image generation API endpoint</name>
  <files>/Users/xuyang/code/my-claudes/app/api/og/route.tsx</files>
  <read_first>
    - (new file) /Users/xuyang/code/my-claudes/app/api/og/route.tsx
  </read_first>
  <action>
    Create `app/api/og/route.tsx` with the following:
    1. Import `ImageResponse` from `next/og` and `NextRequest`
    2. Export `runtime = 'edge'` and `dynamic = 'force-dynamic'`
    3. Implement GET handler that extracts `title` from search params
    4. Generate image with:
       - Vertical gradient background: `#1a1a1a` to `#000000` (dark theme)
       - Post title: 64px, 700 weight, white color, GeistSans font
       - Blog domain: 24px, gray color, `myclaudes.dev` text
       - Dimensions: 1200 × 630 (standard OG size)
    5. Use the JSX template from 04-RESEARCH.md (pattern verified)
  </action>
  <acceptance_criteria>
    - File `app/api/og/route.tsx` exists
    - File contains `export const runtime = 'edge'`
    - File contains `export async function GET(`
    - File contains `width: 1200, height: 630`
    - File uses GeistSans font matching the project
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/xuyang/code/my-claudes/app/api/og/route.tsx && grep -c "runtime = 'edge'" /Users/xuyang/code/my-claudes/app/api/og/route.tsx</automated>
  </verify>
  <done>OG 图片生成端点已创建，使用 edge runtime 和动态渲染</done>
</task>

<task type="auto">
  <name>Task 3: Add generateMetadata to all pages</name>
  <files>
/Users/xuyang/code/my-claudes/app/layout.tsx
/Users/xuyang/code/my-claudes/app/page.tsx
/Users/xuyang/code/my-claudes/app/about/page.tsx
/Users/xuyang/code/my-claudes/app/posts/[slug]/page.tsx
/Users/xuyang/code/my-claudes/app/categories/[category]/page.tsx
/Users/xuyang/code/my-claudes/app/tags/[tag]/page.tsx
  </files>
  <read_first>
    - /Users/xuyang/code/my-claudes/app/layout.tsx
    - /Users/xuyang/code/my-claudes/app/page.tsx
    - /Users/xuyang/code/my-claudes/app/about/page.tsx
    - /Users/xuyang/code/my-claudes/app/posts/[slug]/page.tsx
    - /Users/xuyang/code/my-claudes/app/categories/[category]/page.tsx
    - /Users/xuyang/code/my-claudes/app/tags/[tag]/page.tsx
    - /Users/xuyang/code/my-claudes/lib/content.ts
  </read_first>
  <action>
    For each page, add `export async function generateMetadata()`:

    1. **Root layout (app/layout.tsx)**:
       - Keep existing static metadata as defaults
       - Add openGraph site-wide defaults, twitter card defaults

    2. **Home page (app/page.tsx)**:
       - Title: "MyClaudes - Personal Technology Blog"
       - Description: "A clean, fast static blog about technology, programming, and software development."

    3. **About page (app/about/page.tsx)**:
       - Title: "About - MyClaudes"
       - Description: "About the author of MyClaudes - a personal technology blog."

    4. **Post page (app/posts/[slug]/page.tsx)**:
       - Use post.title for title
       - Use post.description for description
       - Add OG tags: type: 'article', publishedTime: post.date, image from `/api/og?title=...`

    5. **Category page (app/categories/[category]/page.tsx)**:
       - Title: "Category: {category} - MyClaudes"
       - Description: "Browse all posts in the {category} category."

    6. **Tag page (app/tags/[tag]/page.tsx)**:
       - Title: "Tag: {tag} - MyClaudes"
       - Description: "Browse all posts tagged with {tag}."

    All URLs must respect `trailingSlash: true` configuration (end with slash).
    Read site URL from `process.env.NEXT_PUBLIC_SITE_URL` with default fallback.

  </action>
  <acceptance_criteria>
    - `grep -c "export async function generateMetadata" /Users/xuyang/code/my-claudes/app/page.tsx` = 1
    - `grep -c "export async function generateMetadata" /Users/xuyang/code/my-claudes/app/about/page.tsx` = 1
    - `grep -c "export async function generateMetadata" /Users/xuyang/code/my-claudes/app/posts/[slug]/page.tsx` = 1
    - `grep -c "export async function generateMetadata" /Users/xuyang/code/my-claudes/app/categories/[category]/page.tsx` = 1
    - `grep -c "export async function generateMetadata" /Users/xuyang/code/my-claudes/app/tags/[tag]/page.tsx` = 1
    - `grep -c "openGraph" /Users/xuyang/code/my-claudes/app/posts/[slug]/page.tsx` ≥ 1
    - `grep -c "api/og?title" /Users/xuyang/code/my-claudes/app/posts/[slug]/page.tsx` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>grep -n "export async function generateMetadata" /Users/xuyang/code/my-claudes/app/*.tsx /Users/xuyang/code/my-claudes/app/**/*.tsx</automated>
  </verify>
  <done>所有页面都已添加 generateMetadata，文章页面包含动态 OG 图片</done>
</task>

<task type="auto">
  <name>Task 4: Create sitemap generation script and robots.txt</name>
  <files>
/Users/xuyang/code/my-claudes/scripts/generate-sitemap.mjs
/Users/xuyang/code/my-claudes/public/robots.txt
/Users/xuyang/code/my-claudes/.gitignore
/Users/xuyang/code/my-claudes/.env.example
  </files>
  <read_first>
    - (new file) /Users/xuyang/code/my-claudes/scripts/generate-sitemap.mjs
    - (new file) /Users/xuyang/code/my-claudes/public/robots.txt
    - /Users/xuyang/code/my-claudes/.gitignore
    - /Users/xuyang/code/my-claudes/.env.example
  </read_first>
  <action>
    1. Create `scripts/generate-sitemap.mjs`:
       - Import `getAllPosts` from `../lib/content.js`
       - Include all static routes (home, about, categories index, tags index)
       - Include all individual posts
       - Include all individual category pages
       - Include all individual tag pages
       - Generate XML sitemap with proper format
       - Write to `public/sitemap.xml`
       - All URLs end with slash (matches `trailingSlash: true`)

    2. Create `public/robots.txt`:
       - `User-Agent: *`
       - `Allow: /`
       - `Sitemap: {NEXT_PUBLIC_SITE_URL}/sitemap.xml` (use env var or default placeholder)

    3. Add `public/sitemap.xml` to `.gitignore` (generated at build time)

    4. Add `NEXT_PUBLIC_SITE_URL` to `.env.example`

  </action>
  <acceptance_criteria>
    - File `scripts/generate-sitemap.mjs` exists and contains `getAllPosts()`
    - File `public/robots.txt` exists and contains `Sitemap:`
    - File `.gitignore` contains `public/sitemap.xml`
    - File `.env.example` contains `NEXT_PUBLIC_SITE_URL=`
    - Script uses `fs.writeFileSync` to write to `./public/sitemap.xml`
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/xuyang/code/my-claudes/scripts/generate-sitemap.mjs && test -f /Users/xuyang/code/my-claudes/public/robots.txt && grep -n "Sitemap:" /Users/xuyang/code/my-claudes/public/robots.txt</automated>
  </verify>
  <done>sitemap 生成脚本已创建，robots.txt 已添加，gitignore 已更新</done>
</task>

</tasks>

<verification>
- 运行 `node scripts/generate-sitemap.mjs` 确认生成成功
- 检查生成的 sitemap.xml 包含所有文章 URL
- 验证 OG 端点可以通过 curl 访问返回图片
</verification>

<success_criteria>

- 所有页面都有正确的元标签（title, description, Open Graph）
- 每个文章页面都有动态 OG 图片链接
- sitemap.xml 在构建时自动生成，包含所有路由
- robots.txt 存在并且指向 sitemap
- .env.example 包含 NEXT_PUBLIC_SITE_URL </success_criteria>

<output>
After completion, create `.planning/phases/04-seo-search-deployment/04-01-SUMMARY.md`
</output>
