---
phase: 04-seo-search-deployment
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - components/SearchModal.tsx
  - components/Header.tsx
  - components.json
autonomous: false
requirements: [SRCH-02]
user_setup: []

must_haves:
  truths:
    - '搜索图标按钮显示在网站头部'
    - '点击搜索图标打开模态对话框'
    - '模态框包含搜索输入框，自动聚焦'
    - '用户输入时异步更新搜索结果'
    - '点击搜索结果关闭模态框并导航到对应文章'
    - '点击模态框外部或按 Escape 关闭'
  artifacts:
    - path: 'components/SearchModal.tsx'
      provides: '搜索模态弹窗组件，包含完整交互'
      min_lines: 100
    - path: 'components/Header.tsx'
      provides: '添加了搜索触发按钮到头部'
      contains: 'SearchModal'
    - path: 'components.json'
      provides: 'shadcn dialog 组件配置'
      contains: 'dialog'
  key_links:
    - from: 'components/Header.tsx'
      to: 'components/SearchModal.tsx'
      via: 'import and render trigger button'
      pattern: 'SearchModal'
    - from: 'components/SearchModal.tsx'
      to: 'lib/search.ts'
      via: 'import usePagefindSearch hook'
      pattern: 'usePagefindSearch'
    - from: 'components/SearchModal.tsx'
      to: 'shadcn/ui/dialog'
      via: 'import Dialog component'
      pattern: 'Dialog'
---

<objective>
创建搜索 UI 组件：可从任何页面访问的弹窗模态搜索界面

Purpose: 用户可以随时点击头部的搜索图标，弹出搜索框搜索所有文章，点击结果直接跳转。提供良好的用户体验，不需要离开当前页面导航。Output: 创建 SearchModal 组件，添加搜索按钮到 Header，集成 Pagefind 搜索功能。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-seo-search-deployment/04-CONTEXT.md
@.planning/phases/04-seo-search-deployment/04-RESEARCH.md
@.planning/phases/04-seo-search-deployment/04-UI-SPEC.md
@04-02-PLAN.md

<interfaces>
From lib/search.ts:
```typescript
export function usePagefindSearch(query: string): SearchResult[];
```

From 04-UI-SPEC.md interaction contract:

1. Search icon button in header triggers modal
2. Click opens modal, focuses input
3. Results update asynchronously while typing
4. Click outside or Escape closes
5. Click result closes modal and navigates </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn dialog component</name>
  <files>/Users/xuyang/code/my-claudes/components.json /Users/xuyang/code/my-claudes/components/ui/dialog.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/components.json
  </read_first>
  <action>
    1. Run `npx shadcn@latest add dialog` to install the dialog component from shadcn official registry
    2. This adds the necessary component files to `components/ui/dialog.tsx`
    3. Uses radix-ui under the hood which matches project design system
  </action>
  <acceptance_criteria>
    - File `components/ui/dialog.tsx` exists
    - `grep -q "Dialog" /Users/xuyang/code/my-claudes/components.json` confirms dialog in registry
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/xuyang/code/my-claudes/components/ui/dialog.tsx</automated>
  </verify>
  <done>shadcn dialog 组件已安装</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchModal component</name>
  <files>/Users/xuyang/code/my-claudes/components/SearchModal.tsx</files>
  <read_first>
    - (new file) /Users/xuyang/code/my-claudes/components/SearchModal.tsx
    - /Users/xuyang/code/my-claudes/lib/search.ts
    - /Users/xuyang/code/my-claudes/components/ui/dialog.tsx
  </read_first>
  <action>
    Create `components/SearchModal.tsx` with the following:
    1. Import `Dialog`, `DialogContent`, `DialogTitle` from `@/components/ui/dialog`
    2. Import `Search` icon from `lucide-react`
    3. Import `usePagefindSearch` from `@/lib/search`
    4. Component structure:
       - Trigger button: Search icon, 44px minimum click area (accessibility requirement)
       - Modal dialog: contains search input and results list
       - Search input: auto-focus when modal opens, debounced search
       - Results list: shows matching posts with title, click navigates
       - Empty state: "No results found" message as per UI spec
       - Error state: "Search failed to load" message
    5. Follows spacing/typography from 04-UI-SPEC.md:
       - Modal title: 24px, 600 weight
       - Search result title: 18px, 600 weight
       - Search input: 16px, 400 weight
       - Minimum 44px click area for interactive elements
    6. Interactions:
       - Open/close state controlled by useState
       - Query changes trigger search via usePagefindSearch
       - Click result → router.push → close modal
       - Escape key and overlay click close via Dialog component
  </action>
  <acceptance_criteria>
    - File `components/SearchModal.tsx` exists
    - Exports `SearchModal` component
    - Imports `Dialog` from `@/components/ui/dialog`
    - Imports `usePagefindSearch` from `@/lib/search`
    - Contains `Search` icon from lucide-react
    - Contains auto-focus on input when modal opens
    - Click area ≥ 44px for interactive elements
    - Uses Tailwind classes matching 04-UI-SPEC spacing scale
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/xuyang/code/my-claudes/components/SearchModal.tsx && grep -n "usePagefindSearch" /Users/xuyang/code/my-claudes/components/SearchModal.tsx</automated>
  </verify>
  <done>SearchModal 组件已创建，包含完整交互</done>
</task>

<task type="auto">
  <name>Task 3: Add SearchModal trigger to Header</name>
  <files>/Users/xuyang/code/my-claudes/components/Header.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/components/Header.tsx
  </read_first>
  <action>
    1. Import `SearchModal` into `Header.tsx`
    2. Add search icon button to the header (right side, next to theme toggle)
    3. Keep existing header layout and navigation intact
    4. Use Tailwind classes matching the existing header styling (dark mode compatible)
  </action>
  <acceptance_criteria>
    - `grep -q "SearchModal" /Users/xuyang/code/my-claudes/components/Header.tsx`
    - `grep -q "import.*SearchModal" /Users/xuyang/code/my-claudes/components/Header.tsx`
    - Header still contains existing navigation links and theme toggle
  </acceptance_criteria>
  <verify>
    <automated>grep -n "SearchModal" /Users/xuyang/code/my-claudes/components/Header.tsx</automated>
  </verify>
  <done>搜索触发按钮已添加到网站头部，可从任何页面打开搜索</done>
</task>

</tasks>

<verification>
Run `npm run dev` and check:
- Search icon appears in header
- Click icon opens modal with input focused
- Typing shows search results
- Click result navigates
- Click outside closes modal
- Escape key closes modal
</verification>

<success_criteria>

- 搜索图标按钮显示在所有页面的头部
- 点击打开模态弹窗，搜索框自动获得焦点
- 输入搜索词显示匹配的结果列表
- 点击结果导航到文章并关闭弹窗
- 点击外部或 Escape 键关闭弹窗
- 样式与现有设计系统一致（dark mode 兼容）
- 符合可访问性要求（≥44px 点击区域）</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-search-deployment/04-03-SUMMARY.md`
</output>
