---
phase: 02-core-content-infrastructure
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - contentlayer.config.ts
  - .env.example
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04, CONT-05]
must_haves:
  truths:
    - 'Contentlayer dependency is installed and configured'
    - 'Next.js is integrated with Contentlayer plugin'
    - 'Contentlayer can process MDX files from /content/posts/'
  artifacts:
    - path: 'contentlayer.config.ts'
      provides: 'Contentlayer configuration'
      contains: 'defineDocumentType'
      exports: ['default']
    - path: 'next.config.ts'
      provides: 'Next.js with Contentlayer plugin'
      contains: 'withContentlayer'
    - path: 'package.json'
      provides: 'Dependencies installed'
      contains: 'contentlayer'
  key_links:
    - from: 'next.config.ts'
      to: 'contentlayer'
      via: 'withContentlayer wrapper'
      pattern: 'withContentlayer'
user_setup: []
---

<objective>
安装 Contentlayer 依赖并配置与 Next.js 的集成。这是整个内容基础设施的基础，让 Contentlayer 能够发现并处理 MDX 内容文件。

Purpose: 设置内容处理引擎，为后续定义文档类型和提取元数据做准备。Output: 配置好的 Contentlayer 与 Next.js 集成环境。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md @/Users/xuyang/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-content-infrastructure/02-CONTEXT.md
@next.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Contentlayer and Shiki dependencies</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    /Users/xuyang/code/my-claudes/package.json
  </read_first>
  <action>
使用 pnpm 安装以下依赖包：
- `contentlayer` - 核心内容处理库
- `@contentlayer/next` - Next.js 集成插件
- `shiki` - 代码语法高亮库

这些是实现内容处理所需的核心依赖。使用 pnpm add 安装生产依赖。 </action> <verify> <automated> grep -E '"contentlayer"|"@contentlayer/next"|"shiki"' package.json </automated> </verify> <done> - package.json contains "contentlayer" dependency - package.json contains "@contentlayer/next" dependency - package.json contains "shiki" dependency - pnpm install completes without errors </done> </task>

<task type="auto">
  <name>Task 2: Configure Contentlayer with Next.js</name>
  <files>next.config.ts, contentlayer.config.ts, .env.example</files>
  <read_first>
    /Users/xuyang/code/my-claudes/next.config.ts
  </read_first>
  <action>
1. In next.config.ts, import `withContentlayer` from `@contentlayer/next`
2. Wrap the existing Next.js config with `withContentlayer()`
3. Create `contentlayer.config.ts` at project root with basic Contentlayer configuration:
   - Set `contentDirPath` to `content`
   - Leave document types definition for later plan (will be added in 02-02)
4. Add `CONTENTLAYER_ENABLED=true` to `.env.example`

Configuration must follow:

- Contentlayer reads from project root `content/` directory
- Next.js 15 App Router compatibility mode enabled </action> <verify> <automated> grep "withContentlayer" next.config.ts && test -f contentlayer.config.ts </automated> </verify> <done> - next.config.ts exports config wrapped with withContentlayer - contentlayer.config.ts exists at project root - .env.example contains CONTENTLAYER_ENABLED=true </done> </task>

</tasks>

<verification>
After this plan completes, run `next build` to verify Contentlayer integration works without errors.
</verification>

<success_criteria>

- All required dependencies installed successfully
- Contentlayer plugin integrated into Next.js configuration
- contentlayer.config.ts file exists with correct base configuration
- pnpm build completes without configuration errors </success_criteria>

<output>
After completion, create `.planning/phases/02-core-content-infrastructure/02-01-SUMMARY.md`
</output>
