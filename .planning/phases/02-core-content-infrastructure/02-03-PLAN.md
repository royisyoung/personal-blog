---
phase: 02-core-content-infrastructure
plan: 3
type: execute
wave: 2
depends_on: [1]
files_modified:
  - contentlayer.config.ts
  - content/posts/.gitkeep
  - content/posts/hello-world/index.mdx
autonomous: true
requirements: [CONT-05]
must_haves:
  truths:
    - 'Shiki is configured for code syntax highlighting'
    - 'Content directory structure is created per decisions'
    - 'Sample post demonstrates code highlighting works correctly'
  artifacts:
    - path: 'contentlayer.config.patch'
      provides: 'Shiki code highlighting configuration'
      contains: 'shiki'
    - path: 'content/posts'
      provides: 'Content directory'
      contains: '.gitkeep'
    - path: 'content/posts/hello-world/index.mdx'
      provides: 'Sample post with code block'
  key_links:
    - from: 'contentlayer.config.ts'
      to: 'shiki'
      via: 'MDX code highlighting configuration'
      pattern: 'shiki'
user_setup: []
---

<objective>
配置 Shiki 代码语法高亮，并创建内容目录结构和示例文章。此计划交付完整的代码高亮功能并验证配置正确。

Purpose: 文章中的代码块需要正确的语法高亮，Shiki 使用 VSCode 的语法引擎，提供准确、美观的高亮效果。创建目录结构验证内容组织方式符合决策。Output: Shiki 配置完成，内容目录结构就绪，示例文章验证功能正常。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md @/Users/xuyang/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-content-infrastructure/02-CONTEXT.md
@contentlayer.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Shiki code highlighting in Contentlayer</name>
  <files>contentlayer.config.ts</files>
  <read_first>
    /Users/xuyang/code/my-claudes/contentlayer.config.ts
  </read_first>
  <action>
Update contentlayer.config.ts to enable MDX processing with Shiki code highlighting:

1. Add MDX configuration with:
   - Enable `remark-gfm` plugin for GitHub-flavored markdown (tables, strikethrough, etc.)
   - Configure Shiki as the highlighter for code blocks
   - Use `github-light` and `github-dark` themes matching the site's light/dark mode
   - Use the default `class` highlighter (outputs semantic classes for styling)

2. The configuration should:
   - Add `mdx: { process: true }` to the Post document type
   - Configure Shiki in the top-level Contentlayer config
   - Preserve all existing Post document definitions from plan 02 </action> <verify> <automated> grep -E "shiki|remark.\*gfm|highlighter" contentlayer.config.ts </automated> </verify> <done>
   - contentlayer.config.ts has Shiki configured as code highlighter
   - remark-gfm plugin is enabled
   - both light and dark themes configured
   - MDX processing enabled for Post documents </done> </task>

<task type="auto">
  <name>Task 2: Create content directory structure and sample post</name>
  <files>content/posts/.gitkeep, content/posts/hello-world/index.mdx</files>
  <read_first>
  </read_first>
  <action>
1. Create directory structure: `content/posts/`
2. Add `.gitkeep` file to `content/posts/` so empty directory is tracked by Git
3. Create sample post: `content/posts/hello-world/index.mdx` with:
   - Valid frontmatter matching schema:
     - title: "Hello World"
     - date: 2025-01-01
     - description: "First post on the blog"
     - category: "meta"
     - tags: ["hello", "introduction"]
   - MDX content including a code block with TypeScript to verify highlighting works

Follow the decision: each post gets its own subfolder `/content/posts/{slug}/index.mdx` so images can be placed alongside content. </action> <verify> <automated> test -f content/posts/.gitkeep && test -f content/posts/hello-world/index.mdx </automated> </verify> <done> - content/posts/ directory exists - content/posts/.gitkeep exists - content/posts/hello-world/index.mdx exists with valid frontmatter - sample post includes at least one code block for testing - directory structure matches decision: {slug}/index.mdx </done> </task>

</tasks>

<verification>
Run `pnpm build` after this plan. The build should complete successfully with:
1. Contentlayer processes the sample MDX post
2. Shiki highlights the code block correctly
3. No validation errors (frontmatter is correct)
</verification>

<success_criteria>

- Shiki correctly configured for code block syntax highlighting
- Content directory structure matches the organization decision
- Sample post with valid frontmatter and code block can be processed
- Build completes successfully with Shiki highlighting working
- Frontmatter validation correctly accepts valid frontmatter (fails if invalid - test by intent if needed) </success_criteria>

<output>
After completion, create `.planning/phases/02-core-content-infrastructure/02-03-SUMMARY.md`
</output>
