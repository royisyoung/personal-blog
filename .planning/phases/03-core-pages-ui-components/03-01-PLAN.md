---
phase: 03-core-pages-ui-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - components/Header.tsx
  - components/ThemeToggle.tsx
  - components/Container.tsx
  - lib/theme.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - 'Site header with navigation is visible on all pages'
    - 'User can toggle between light and dark themes'
    - 'User theme preference persists across page visits'
    - 'Layout is responsive and adapts to mobile, tablet, desktop'
    - 'Content is constrained to 65-70ch for optimal readability'
  artifacts:
    - path: 'components/Header.tsx'
      provides: 'Site header with blog title and navigation links'
      min_lines: 40
    - path: 'components/ThemeToggle.tsx'
      provides: 'Sun/moon icon button for theme switching'
      min_lines: 50
    - path: 'components/Container.tsx'
      provides: 'Constrained width container with centered content'
      min_lines: 20
    - path: 'lib/theme.ts'
      provides: 'Theme context and persistence utilities'
      min_lines: 30
  key_links:
    - from: 'app/layout.tsx'
      to: 'components/Header.tsx'
      via: 'import and render in root layout'
      pattern: 'import.*Header'
    - from: 'components/ThemeToggle.tsx'
      to: 'localStorage'
      via: 'persists theme preference'
      pattern: 'localStorage'
    - from: 'app/layout.tsx'
      to: 'components/Container.tsx'
      via: 'wraps all page content'
      pattern: 'import.*Container'
---

<objective>
Create shared layout foundation: root layout with header navigation and theme toggle.

Purpose: Establishes the base layout structure that all other pages will build upon. Output: Header component with navigation, theme toggle with persistence, constrained container for content. </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md @/Users/xuyang/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-ui-components/03-CONTEXT.md
@.planning/phases/03-core-pages-ui-components/03-UI-SPEC.md
@app/layout.tsx
@lib/content.ts

<interfaces>
From app/layout.tsx:
```typescript
export default function RootLayout({ children }: { children: React.ReactNode })
```

From lib/content.ts:

```typescript
export function getAllPosts(): Post[];
export function getPostBySlug(slug: string): Post | undefined;
```

From contentlayer.config.ts:

```typescript
export interface Post {
  title: string;
  date: Date;
  description: string;
  category: string;
  tags: string[];
  slug: string;
  body: string;
}
```

</interfaces>

Design decisions from CONTEXT.md:

- Minimal top header: blog title on left, nav links on right, theme toggle far right
- Narrow content: 65-70ch (characters) maximum width for readability
- Icon button (sun/moon) for theme toggle
- Saves preference to localStorage
- Color: zinc-50/zinc-950 (background), zinc-100/zinc-900 (nav), zinc-900/zinc-100 (accent)
- Spacing follows UI-SPEC scale: xs=4, sm=8, md=16, lg=24, xl=32 </context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme context and utility functions</name>
  <files>lib/theme.ts</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/lib/theme.ts (does not exist yet)
    - /Users/xuyang/code/my-claudes/app/layout.tsx
  </read_first>
  <action>
Create `lib/theme.ts` with:
1. Create a `Theme` type (`'light' | 'dark'`)
2. Create `ThemeContext` and `ThemeProvider` component
3. Create `useTheme()` hook to access current theme and toggle function
4. Implement `getInitialTheme()` that reads from localStorage or system preference
5. Implement `saveTheme()` that writes theme preference to localStorage

Use React context API with `useState` and `useEffect` for hydration compatibility. </action> <acceptance_criteria> - `lib/theme.ts` file exists - File exports `ThemeProvider`, `useTheme`, `Theme` type - File contains `localStorage` access for `theme-preference` key - `grep -n "getInitialTheme" lib/theme.ts` finds the function </acceptance_criteria> <verify> <automated>ls lib/theme.ts && grep -q "export.\*ThemeProvider" lib/theme.ts</automated> </verify> <done> Theme context provider exists with persistent storage and system preference detection. </done> </task>

<task type="auto">
  <name>Task 2: Create shared components (Container + Header)</name>
  <files>components/Container.tsx, components/Header.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/components/Container.tsx (does not exist yet)
    - /Users/xuyang/code/my-claudes/components/Header.tsx (does not exist yet)
    - /Users/xuyang/code/my-claudes/app/layout.tsx
  </read_first>
  <action>
1. Create `components/Container.tsx`:
   - Max-width constrained to 70ch (narrow for readability)
   - Auto margins for horizontal centering
   - Horizontal padding: 1rem (16px = md) on mobile
   - Component accepts `children` and `className` props
   - Follows spacing from UI-SPEC: padding-y 3rem (48px = 2xl) top/bottom by default

2. Create `components/Header.tsx`:
   - Blog title on the left (links to home page)
   - Navigation links on right: Home → `/`, Categories → `/categories`, About → `/about`
   - Uses flex layout with justify-between items-center
   - Padding: 1rem (md) horizontal on mobile, 1.5rem on desktop
   - Active link styling: current page has accent color (zinc-900 in light, zinc-100 in dark)
   - Follows shadcn button styling conventions for navigation links </action> <acceptance_criteria>
   - `components/Container.tsx` exists and exports default Container component
   - `components/Header.tsx` exists and exports default Header component
   - Container CSS class contains `max-w-[70ch]`
   - Header contains navigation links to `/`, `/categories`, `/about` </acceptance_criteria> <verify> <automated>ls components/Container.tsx components/Header.tsx && grep -q "max-w-\[70ch\]" components/Container.tsx</automated> </verify> <done> Container component constrains content width for readability; Header component displays navigation links consistently across all pages. </done> </task>

<task type="auto">
  <name>Task 3: Create ThemeToggle button component and update root layout</name>
  <files>components/ThemeToggle.tsx, app/layout.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/components/ThemeToggle.tsx (does not exist yet)
    - /Users/xuyang/code/my-claudes/app/layout.tsx
    - /Users/xuyang/code/my-claudes/lib/theme.ts
    - /Users/xuyang/code/my-claudes/components/ui/button.tsx
  </read_first>
  <action>
1. Create `components/ThemeToggle.tsx`:
   - Use shadcn Button component with variant="ghost" size="icon"
   - Uses Sun and Moon icons from lucide-react (need to import)
   - Clicking toggles between light and dark theme
   - Uses context from `useTheme()` hook
   - 44px touch target (accessibility requirement from UI-SPEC)
   - Add `aria-label="Toggle theme"` for accessibility

2. Update `app/layout.tsx`:
   - Import and add `ThemeProvider` wrapping children
   - Import and add `Header` component above main content
   - Import and add `Container` component wrapping children content
   - Apply dark class to html element based on current theme
   - Keep existing Geist font setup </action> <acceptance_criteria>
   - `components/ThemeToggle.tsx` exists and exports default ThemeToggle component
   - `app/layout.tsx` imports ThemeProvider, Header, Container
   - `app/layout.tsx` applies dark class to html based on theme state
   - ThemeToggle imports Sun and Moon icons from lucide-react
   - ThemeToggle has aria-label="Toggle theme" </acceptance_criteria> <verify> <automated>ls components/ThemeToggle.tsx && grep -n "ThemeProvider" app/layout.tsx</automated> </verify> <done> Theme toggle works, preference saves to localStorage, persists across page refresh, dark mode applies correctly to html root. </done> </task>

</tasks>

<verification>
All shared layout components are created and integrated into root layout.
</verification>

<success_criteria>

1. Root layout renders Header at top of every page
2. Theme toggle button toggles between light/dark modes
3. Theme preference persists after page refresh
4. Content is constrained to 70ch maximum width
5. Layout responds correctly to different screen sizes </success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-ui-components/03-01-SUMMARY.md`
</output>
