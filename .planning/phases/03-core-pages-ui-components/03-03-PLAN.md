---
phase: 03-core-pages-ui-components
plan: 03
type: execute
wave: 2
depends_on:
  - '03-01'
  - '03-02'
files_modified:
  - app/posts/[slug]/page.tsx
  - app/categories/[category]/page.tsx
  - app/tags/[tag]/page.tsx
  - components/Pagination.tsx
  - lib/pagination.ts
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - UI-05
must_haves:
  truths:
    - 'Clicking a post opens individual post page with full MDX content rendered'
    - 'Category page displays posts filtered by the selected category'
    - 'Tag page displays posts filtered by the selected tag'
    - 'Pagination works for post lists when there are more posts than posts per page'
    - 'MDX content has correct syntax highlighting for code blocks'
  artifacts:
    - path: 'app/posts/[slug]/page.tsx'
      provides: 'Individual post page with full MDX rendering'
      exports: ['default', 'generateStaticParams']
    - path: 'app/categories/[category]/page.tsx'
      provides: 'Category-filtered post listing page'
      exports: ['default', 'generateStaticParams']
    - path: 'app/tags/[tag]/page.tsx'
      provides: 'Tag-filtered post listing page'
      exports: ['default', 'generateStaticParams']
    - path: 'components/Pagination.tsx'
      provides: 'Pagination UI with previous/next and page numbers'
      min_lines: 50
    - path: 'lib/pagination.ts'
      provides: 'Pagination calculation utilities'
      min_lines: 30
  key_links:
    - from: 'app/posts/[slug]/page.tsx'
      to: 'contentlayer/generated'
      via: 'uses getPostBySlug to retrieve post'
      pattern: 'getPostBySlug'
    - from: 'app/posts/[slug]/page.tsx'
      via: 'renders MDX content from post.body'
      pattern: 'MDXContent'
    - from: 'app/categories/[category]/page.tsx'
      to: 'components/PostList.tsx'
      via: 'reuses PostList component for filtered posts'
      pattern: 'import.*PostList'
    - from: 'app/tags/[tag]/page.tsx'
      to: 'components/PostList.tsx'
      via: 'reuses PostList component for filtered posts'
      pattern: 'import.*PostList'
    - from: 'PostList pages'
      to: 'components/Pagination.tsx'
      via: 'renders pagination when post count exceeds posts per page'
      pattern: 'import.*Pagination'
---

<objective>
Implement dynamic routes: individual post page, category filtering page, tag filtering page, and pagination.

Purpose: Completes all core page routes required by Phase 3. All content paths now work end-to-end. Output: Post detail page with MDX rendering, category/tag filtered pages, pagination system. </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md @/Users/xuyang/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-ui-components/03-CONTEXT.md
@.planning/phases/03-core-pages-ui-components/03-UI-SPEC.md
@lib/content.ts
@components/PostList.tsx
@components/Container.tsx

<interfaces>
From lib/content.ts:
```typescript
export function getAllPosts(): Post[]
export function getPostBySlug(slug: string): Post | undefined
```

From 03-01 and 03-02 outputs:

```typescript
export default function Container(props);
export default function PostList(props: { posts: Post[] });
export type Post = {
  title: string;
  date: Date;
  description: string;
  category: string;
  tags: string[];
  slug: string;
  body: string;
};
```

Design decisions from CONTEXT.md:

- Traditional pagination with page number buttons (infinite scroll deferred)
- Same post list layout as home page for consistency on category/tag pages
- Page title at top shows "Category: {name}" or "Tag: {name}"
- Traditional pagination is simpler for static blog SSG architecture
- Contentlayer already configured with Shiki for code syntax highlighting </context>

<tasks>

<task type="auto">
  <name>Task 1: Create pagination utility functions and component</name>
  <files>lib/pagination.ts, components/Pagination.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/lib/pagination.ts (new)
    - /Users/xuyang/code/my-claudes/components/Pagination.tsx (new)
    - /Users/xuyang/code/my-claudes/components/ui/button.tsx
  </read_first>
  <action>
1. Create `lib/pagination.ts`:
   - Define `postsPerPage = 10` (constant)
   - Export `calculatePagination(totalPosts: number, currentPage: number)` that returns:
     - `totalPages` - total number of pages
     - `currentPage` - current page number (1-indexed)
     - `prevPage` - previous page number or null
     - `nextPage` - next page number or null
     - `pages` - array of page numbers to display
   - Export `getPaginatedPosts(posts: Post[], page: number)` returns slice for the page

2. Create `components/Pagination.tsx`:
   - Accepts props: `currentPage`, `totalPages`, `basePath` (e.g., "/", "/categories/react")
   - Renders Previous button (disabled if on first page)
   - Renders Next button (disabled if on last page)
   - Renders page number buttons, highlights current page with accent color
   - Uses existing shadcn Button component from components/ui/button
   - Uses Next.js Link for navigation </action> <acceptance_criteria>
   - `lib/pagination.ts` exists and exports `calculatePagination`, `getPaginatedPosts`
   - `lib/pagination.ts` defines `postsPerPage = 10` constant
   - `components/Pagination.tsx` exists and exports Pagination component
   - Pagination uses Button component from shadcn and Link from Next.js
   - Pagination displays Previous/Next buttons and page numbers </acceptance_criteria> <verify> <automated>ls lib/pagination.ts components/Pagination.tsx && grep -q "postsPerPage" lib/pagination.ts</automated> </verify> <done> Pagination utilities and UI component created with pagination calculations and navigation. </done> </task>

<task type="auto">
  <name>Task 2: Implement individual post page (app/posts/[slug]/page.tsx)</name>
  <files>app/posts/[slug]/page.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/app/posts/[slug]/page.tsx (new)
    - /Users/xuyang/code/my-claudes/lib/content.ts
    - /Users/xuyang/code/my-claudes/components/Container.tsx
    - /Users/xuyang/code/my-claudes/contentlayer.config.ts
  </read_first>
  <action>
Create `app/posts/[slug]/page.tsx`:
   - Implement `generateStaticParams()` - gets all slugs from allPosts for static generation
   - Default component: get post by slug from params
   - If post not found: return 404-style message "Post not found"
   - Render:
     - Post title (28px, semibold)
     - Metadata: formatted date + category
     - 24px (lg) spacing after metadata
     - MDX content rendered with syntax highlighting (use Contentlayer's MDXContent)
     - Spacing between elements follows UI-SPEC
   - Use Container for width constraint
   - All code blocks already have Shiki highlighting from Phase 2 config
  </action>
  <acceptance_criteria>
    - `app/posts/[slug]/page.tsx` exists
    - File exports `generateStaticParams` function
    - File exports default PostPage component
    - Uses `getPostBySlug` from `lib/content.ts`
    - Renders `MDXContent` for the post body
    - Displays post title, date, and category
  </acceptance_criteria>
  <verify>
    <automated>ls app/posts/[slug]/page.tsx && grep -q "generateStaticParams" app/posts/[slug]/page.tsx</automated>
  </verify>
  <done>
Individual post page renders full MDX content with correct metadata and syntax highlighting.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement category page and tag page</name>
  <files>app/categories/[category]/page.tsx, app/tags/[tag]/page.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/app/categories/[category]/page.tsx (new)
    - /Users/xuyang/code/my-claudes/app/tags/[tag]/page.tsx (new)
    - /Users/xuyang/code/my-claudes/lib/content.ts
    - /Users/xuyang/code/my-claudes/components/PostList.tsx
    - /Users/xuyang/code/my-claudes/components/Container.tsx
    - /Users/xuyang/code/my-claudes/components/Pagination.tsx
    - /Users/xuyang/code/my-claudes/lib/pagination.ts
  </read_first>
  <action>
1. Create `app/categories/[category]/page.tsx`:
   - `generateStaticParams()` extracts all unique categories from allPosts
   - Filter posts: `allPosts.filter(post => post.category === params.category)`
   - Page title: "Category: {category}" with capitalization
   - Reuse PostList component for filtered posts (same layout as home page)
   - Add pagination when filtered posts exceed postsPerPage
   - Base path for pagination: `/categories/${params.category}`
   - Use Container for width constraint

2. Create `app/tags/[tag]/page.tsx`:
   - `generateStaticParams()` extracts all unique tags from allPosts
   - Filter posts: `allPosts.filter(post => post.tags.includes(params.tag))`
   - Page title: "Tag: {tag}" with capitalization
   - Reuse PostList component (same layout consistency)
   - Add pagination with base path `/tags/${params.tag}`
   - Use Container for width constraint </action> <acceptance_criteria>
   - `app/categories/[category]/page.tsx` exists with generateStaticParams
   - `app/tags/[tag]/page.tsx` exists with generateStaticParams
   - Both files import and reuse PostList component
   - Both files import and use Pagination component
   - Category page filters posts by category field
   - Tag page filters posts by tags array includes tag
   - Page title displays "Category: {name}" and "Tag: {name}" </acceptance_criteria> <verify> <automated>ls app/categories/[category]/page.tsx app/tags/[tag]/page.tsx && grep -q "generateStaticParams" app/categories/[category]/page.tsx</automated> </verify> <done> Category page and tag page work - posts can be filtered by category and tag with pagination. </done> </task>

</tasks>

<verification>
All core pages implemented: post detail, category filter, tag filter, pagination.
</verification>

<success_criteria>

1. Every post has a dedicated page at /posts/{slug} with full content rendered
2. MDX code blocks have correct syntax highlighting that adapts to theme
3. /categories/{category} shows all posts in that category with same post list layout
4. /tags/{tag} shows all posts with that tag with same post list layout
5. Pagination appears when post count exceeds 10 posts per page
6. All links navigate correctly between pages
7. Static generation works at build time for all posts, categories, and tags </success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-ui-components/03-03-SUMMARY.md`
</output>
