---
phase: 03-core-pages-ui-components
plan: 02
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - app/page.tsx
  - app/about/page.tsx
  - components/PostList.tsx
  - components/PostListItem.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-05
  - UI-01
  - UI-02
must_haves:
  truths:
    - 'Home page displays blog description and sorted list of all posts'
    - 'About page displays author information'
    - 'Post list shows title + date + category + description excerpt'
    - 'Posts are sorted by date descending (newest first)'
    - 'Layout is responsive and minimalist focusing on content'
  artifacts:
    - path: 'app/page.tsx'
      provides: 'Home page with blog description and post list'
      exports: ['default']
    - path: 'app/about/page.tsx'
      provides: 'About page for author information'
      exports: ['default']
    - path: 'components/PostList.tsx'
      provides: 'Renders list of PostListItem components'
      min_lines: 30
    - path: 'components/PostListItem.tsx'
      provides: 'Single post entry with metadata and excerpt'
      min_lines: 40
  key_links:
    - from: 'app/page.tsx'
      to: 'lib/content.ts'
      via: 'calls getAllPosts() for sorted post list'
      pattern: 'getAllPosts'
    - from: 'app/page.tsx'
      to: 'components/PostList.tsx'
      via: 'renders PostList with all posts'
      pattern: 'import.*PostList'
    - from: 'components/PostList.tsx'
      to: 'components/PostListItem.tsx'
      via: 'maps posts to PostListItem components'
      pattern: 'import.*PostListItem'
    - from: 'components/PostListItem'
      to: 'app/posts/[slug]/page.tsx'
      via: 'links to individual post page'
      pattern: 'href.*posts'
---

<objective>
Implement home page with post list and about page. Reusable post list components created that will be reused on category and tag pages.

Purpose: Delivers the main entry point to the blog and author information. Output: Home page with sorted post list, about page, reusable post list components. </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md @/Users/xuyang/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-ui-components/03-CONTEXT.md
@.planning/phases/03-core-pages-ui-components/03-UI-SPEC.md
@app/layout.tsx
@lib/content.ts
@components/Container.tsx
@components/Header.tsx

<interfaces>
From lib/content.ts:
```typescript
export function getAllPosts(): Post[]
export function getPostBySlug(slug: string): Post | undefined
```

From 03-01-PLAN outputs:

```typescript
export default function Container({ children, className }: Props);
export default function Header();
export default function ThemeToggle();
```

Design decisions from CONTEXT.md:

- Home post list: List with excerpt format (title + date + category + description)
- Not pure text list, not full card format — balanced minimalist design
- Same post list layout reused across home/category/tag pages for consistency
- Reading time deferred to v2 (Phase 5) — keep v1 simple
- Typography: Body 16px 1.5 line-height, Heading 28px 1.2 line-height
- Color: Accent (links) zinc-900 / zinc-100
- Spacing: xs=4, sm=8, md=16, lg=24, xl=32 </context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostListItem and PostList reusable components</name>
  <files>components/PostListItem.tsx, components/PostList.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/components/PostListItem.tsx (new)
    - /Users/xuyang/code/my-claudes/components/PostList.tsx (new)
    - /Users/xuyang/code/my-claudes/lib/content.ts
    - /Users/xuyang/code/my-claudes/contentlayer.config.ts
  </read_first>
  <action>
1. Create `components/PostListItem.tsx`:
   - Accepts `post` prop of type `Post`
   - Renders:
     - Post title as heading (20px, semibold) linked to `/posts/${post.slug}`
     - Date (formatted as `YYYY-MM-DD`) and category displayed below title
     - Description excerpt as body text
   - Spacing: 16px (md) bottom margin between items, 8px (sm) vertical spacing between elements
   - Date and category text color: muted (zinc-500 / zinc-400 in dark)
   - Uses Next.js `Link` component for navigation

2. Create `components/PostList.tsx`:
   - Accepts `posts` prop of type `Post[]`
   - Maps over posts and renders `PostListItem` for each
   - If posts array is empty, shows "No posts yet" message per UI-SPEC
   - Wraps in `<div className="space-y-8">` (8px = sm between posts) </action> <acceptance_criteria>
   - `components/PostListItem.tsx` exists and exports PostListItem
   - `components/PostList.tsx` exists and exports PostList
   - PostListItem contains link to `/posts/${post.slug}`
   - PostListItem displays title, formatted date, category, description
   - PostList shows "No posts yet" when posts array is empty
   - PostListItem imports Post type from contentlayer/generated </acceptance_criteria> <verify> <automated>ls components/PostListItem.tsx components/PostList.tsx && grep -q "href.*posts.*slug" components/PostListItem.tsx</automated> </verify> <done> Reusable post list components created that display post metadata with correct formatting and spacing. </done> </task>

<task type="auto">
  <name>Task 2: Implement home page (app/page.tsx)</name>
  <files>app/page.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/app/page.tsx (existing, will replace)
    - /Users/xuyang/code/my-claudes/lib/content.ts
    - /Users/xuyang/code/my-claudes/components/PostList.tsx
    - /Users/xuyang/code/my-claudes/components/Container.tsx
  </read_first>
  <action>
Replace the default Next.js homepage with:
   - Heading: Blog title (from site configuration, use "Personal Tech Blog" for now)
   - Short blog description paragraph (use "A minimalist technical blog about software development." for now)
   - Spacing: 32px (xl) between description and post list heading
   - Post list heading "Recent Posts" (Secondary Heading: 20px, semibold)
   - Render PostList with all posts from getAllPosts()
   - Posts already sorted by date descending by getAllPosts()
   - Use Container component for content width constraints
   - Keep all spacing per UI-SPEC: 48px (2xl) top padding, 32px (xl) between sections
  </action>
  <acceptance_criteria>
    - app/page.tsx imports getAllPosts from lib/content.ts
    - app/page.tsx imports and renders PostList
    - app/page.tsx uses Container component
    - Default Next.js Vercel/Next.js images are removed
    - File exports default Home component that renders page content
  </acceptance_criteria>
  <verify>
    <automated>grep -q "getAllPosts" app/page.tsx && grep -q "PostList" app/page.tsx && ! grep -q "vercel.svg" app/page.tsx</automated>
  </verify>
  <done>
Home page displays blog description and all posts sorted by date newest first.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement about page (app/about/page.tsx)</name>
  <files>app/about/page.tsx</files>
  <read_first>
    - /Users/xuyang/code/my-claudes/app/about/page.tsx (new)
    - /Users/xuyang/code/my-claudes/components/Container.tsx
  </read_first>
  <action>
Create `app/about/page.tsx`:
   - Page heading "About" (28px, semibold)
   - Author information content (use placeholder content: "Author name and bio goes here. This is a personal technical blog about software development, programming, and technology.")
   - Content rendered in Container with correct spacing
   - Use same typography as body text: 16px, 1.5 line-height
   - Spacing: 24px (lg) between heading and content
  </action>
  <acceptance_criteria>
    - `app/about/page.tsx` file exists
    - File exports default AboutPage component
    - Page contains heading "About"
    - Page uses Container component
    - Author bio placeholder content included
  </acceptance_criteria>
  <verify>
    <automated>ls app/about/page.tsx && grep -q "export default" app/about/page.tsx && grep -q "About" app/about/page.tsx</automated>
  </verify>
  <done>
About page exists displaying author information.
  </done>
</task>

</tasks>

<verification>
Home page, about page, and reusable post components complete.
</verification>

<success_criteria>

1. Home page loads and displays blog description
2. All posts from content directory appear in date-descending order
3. Each post shows title, date, category, and description excerpt
4. Clicking post title navigates to (currently non-existent) post page
5. About page is accessible at /about and shows author placeholder content
6. Layout is responsive, content constrained to 70ch width </success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-ui-components/03-02-SUMMARY.md`
</output>
