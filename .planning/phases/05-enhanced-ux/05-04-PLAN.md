---
phase: 05-enhanced-ux
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - components/RelatedPosts.tsx
  - lib/rehype-wrap-code-blocks.ts
  - contentlayer.config.ts
  - app/posts/[slug]/page.tsx
autonomous: true
requirements: [UX-03, UX-05]
user_setup: []

must_haves:
  truths:
    - 'Every code block has a copy button at top-right corner'
    - 'Clicking copy button copies code and shows visual checkmark feedback'
    - '2-3 related posts show at end of article sorted by shared tags'
  artifacts:
    - path: 'components/RelatedPosts.tsx'
      provides: 'Related posts card component for end of article'
    - path: 'lib/rehype-wrap-code-blocks.ts'
      provides: 'Rehype plugin to wrap code blocks with relative container for copy button'
    - path: 'app/posts/[slug]/page.tsx'
      provides: 'RelatedPosts rendered after article content'
  key_links:
    - from: 'contentlayer.config.ts'
      to: 'lib/rehype-wrap-code-blocks.ts'
      via: 'rehype plugin wraps pre elements'
    - from: 'wrapped pre'
      to: 'CopyButton'
      via: 'relative positioning allows absolute button placement'
    - from: 'app/posts/[slug]/page.tsx'
      to: 'lib/related-posts.ts'
      via: 'calls getRelatedPosts to get sorted list'
---

<objective>
集成复制按钮到代码块（通过 rehype plugin），并实现相关文章展示组件。

Purpose: 一键复制让读者更方便复制代码示例，相关文章让读者可以继续阅读相关内容。

Output: Rehype plugin wraps code blocks, copy button injected, related posts component at end of article. </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-enhanced-ux/05-CONTEXT.md
@.planning/phases/05-enhanced-ux/05-RESEARCH.md
@contentlayer.config.ts
@components/CopyButton.tsx
@lib/related-posts.ts
@app/posts/[slug]/page.tsx

<interfaces>
From components/CopyButton.tsx (already created in 05-01):
```typescript
export function CopyButton({ text }: { text: string }): React.ReactNode;
```

From lib/related-posts.ts (already created in 05-02):

```typescript
export function getRelatedPosts(currentPost: Post, allPosts: Post[], limit = 3): Post[];
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rehype plugin to wrap code blocks for copy button</name>
  <read_first>
    contentlayer.config.ts
    components/CopyButton.tsx
  </read_first>
  <action>
1. Create `lib/rehype-wrap-code-blocks.ts` with a rehype plugin that:
   - Traverses the AST
   - Finds all `<pre>` elements that contain `<code>` (code blocks)
   - Wraps them in a parent `<div>` with `position: relative` class (for absolute positioning of copy button)
   - Injects the CopyButton component as React component with the code text as prop

Wait: We cannot directly inject React components into MDX because it's already compiled. Alternative approach:

- Wrap the `<pre>` in a `div` with class `code-block-wrapper relative`
- Add a `button` element with `data-code="<the code>"` attribute
- Our client-side CopyButton component will hydrate it after mount

Alternative approach steps:

- For each pre element:
  - Get the text content of the code inside
  - Wrap the pre in a `<div class="code-block-wrapper relative">`
  - Add a placeholder `<button data-copy-button data-code="${text}">`
- In CopyButton component client-side: useEffect finds all placeholders and hydrates them with proper React copy handlers

2. Add this plugin to `rehypePlugins` array in `contentlayer.config.ts` after Shiki. </action> <verify> <automated> grep -q "rehypeWrapCodeBlocks" lib/rehype-wrap-code-blocks.ts && grep -q "rehypeWrapCodeBlocks" contentlayer.config.ts && npm run lint -- --quiet </automated> </verify> <done>

- Rehype plugin exported from lib/rehype-wrap-code-blocks.ts
- Plugin finds all pre > code blocks
- Wraps each in div with relative positioning
- Passes the code text as data attribute for hydration
- Plugin added to contentlayer.config.ts after Shiki plugin
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- lib/rehype-wrap-code-blocks.ts contains `export function rehypeWrapCodeBlocks`
- lib/rehype-wrap-code-blocks.ts contains `code-block-wrapper`
- lib/rehype-wrap-code-blocks.ts contains `data-copy-button`
- contentlayer.config.ts contains `rehypeWrapCodeBlocks` in rehypePlugins
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 2: Hydrate copy buttons client-side</name>
  <read_first>
    components/CopyButton.tsx
    lib/rehype-wrap-code-blocks.ts
  </read_first>
  <action>
Modify `components/CopyButton.tsx` to add a root hydration component:

1. Add new export `CopyButtonHydrator` as a client component
2. `CopyButtonHydrator` has no props
3. In useEffect: after component mounts on client, select all `[data-copy-button]` elements
4. For each element: get the `data-code` content, replace the placeholder with a rendered React CopyButton
5. Use `useEffect` with empty dependency array to run once after mount
6. Add `CopyButtonHydrator` to the post page (`app/posts/[slug]/page.tsx`) - render it at the end of the article for client-side hydration

This approach works because MDX outputs static HTML, we hydrate the copy buttons client-side after page load. </action> <verify> <automated> grep -q "CopyButtonHydrator" components/CopyButton.tsx && grep -q "CopyButtonHydrator" app/posts/[slug]/page.tsx && npm run lint -- --quiet </automated> </verify> <done>

- CopyButtonHydrator exported from components/CopyButton.tsx
- Hydrator runs useEffect to find and hydrate all placeholders
- Each placeholder gets replaced with interactive CopyButton
- Hydrator added to post page for client-side execution
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- components/CopyButton.tsx contains `export function CopyButtonHydrator`
- components/CopyButton.tsx contains `querySelectorAll.*data-copy-button`
- components/CopyButton.tsx contains `useEffect`
- app/posts/[slug]/page.tsx contains `CopyButtonHydrator`
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 3: Implement RelatedPosts component and integrate into post page</name>
  <read_first>
    app/posts/[slug]/page.tsx
    lib/related-posts.ts
    lib/content.ts
  </read_first>
  <action>
1. Create `components/RelatedPosts.tsx` server component:
   - Props: `{ currentPost: Post }`
   - Import `getAllPosts` from `@/lib/content` and `getRelatedPosts` from `@/lib/related-posts`
   - Get related posts: `getRelatedPosts(currentPost, getAllPosts(), 3)`
   - Render a section with heading "Related Posts"
   - Display each related post as card with: title, description excerpt, date
   - Link to the related post page
   - Use subtle styling: bordered cards, hover effect

2. Add the RelatedPosts component to `app/posts/[slug]/page.tsx` after the article content, before closing article tag. </action> <verify> <automated> grep -q "RelatedPosts" components/RelatedPosts.tsx && grep -q "RelatedPosts" app/posts/[slug]/page.tsx && npm run lint -- --quiet </automated> </verify> <done>

- RelatedPosts component created, gets related posts using getRelatedPosts
- Displays 2-3 related posts at end of article
- Each post card shows title, excerpt, date
- Cards are linked to actual post pages
- Component added to post page after article content
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- components/RelatedPosts.tsx contains `export function RelatedPosts`
- components/RelatedPosts.tsx contains `getRelatedPosts(`
- components/RelatedPosts.tsx contains `Related Posts`
- app/posts/[slug]/page.tsx contains `<RelatedPosts currentPost={post}`
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

</tasks>

<verification>
Verify that after build, all code blocks are wrapped and copy button is present and functional. Check that code text is correctly extracted and copied.
</verification>

<success_criteria>

- All code blocks wrapped in relative container
- Copy button appears on every code block at top-right
- Click copy copies exact code text to clipboard
- Visual feedback (icon changes to checkmark) after copy
- Related posts display at end of article sorted by shared tags
- Related posts cards link correctly
- No hydration errors, build succeeds </success_criteria>

<output>
After completion, create `.planning/phases/05-enhanced-ux/05-04-SUMMARY.md` with implementation summary.
</output>
