---
phase: 05-enhanced-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ReadingProgressBar.tsx
  - components/BackToTopButton.tsx
  - components/CopyButton.tsx
  - app/posts/[slug]/page.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03]
user_setup: []

must_haves:
  truths:
    - 'Reading progress bar displays at top of page and updates during scrolling'
    - 'Back to top button appears in bottom-right corner and smooth scrolls to top when clicked'
    - 'Each code block has a copy button at top-right that copies code to clipboard with visual feedback'
  artifacts:
    - path: 'components/ReadingProgressBar.tsx'
      provides: 'Reading progress bar client component'
      min_lines: 25
    - path: 'components/BackToTopButton.tsx'
      provides: 'Back to top button client component with fade-in'
      min_lines: 40
    - path: 'components/CopyButton.tsx'
      provides: 'Copy button client component with clipboard integration'
      min_lines: 30
  key_links:
    - from: 'app/posts/[slug]/page.tsx'
      to: 'components/ReadingProgressBar.tsx'
      via: 'import and render in article'
    - from: 'app/posts/[slug]/page.tsx'
      to: 'components/BackToTopButton.tsx'
      via: 'import and render in article'
---

<objective>
实现两个基础客户端交互功能：阅读进度条和返回顶部按钮。

这两个功能都是纯客户端交互，不依赖内容层修改，可以独立实现，验证开发流程正常工作。

Purpose: 提供基本的阅读导航辅助，让用户在长文章中更方便定位位置和回到开头。Output: 两个可工作的客户端组件，集成到文章页面。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-enhanced-ux/05-CONTEXT.md
@.planning/phases/05-enhanced-ux/05-RESEARCH.md
@app/posts/[slug]/page.tsx

<interfaces>
From app/posts/[slug]/page.tsx:
```typescript
export default async function PostPage({ params }: PostPageProps) {
  // Server component - renders article layout
}
```

Research provides reference implementation:

```typescript
'use client';
export function ReadingProgressBar() { ... }
'use client';
export function BackToTopButton() { ... }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Reading Progress Bar component</name>
  <read_first>
    app/posts/[slug]/page.tsx
  </read_first>
  <action>
Create `components/ReadingProgressBar.tsx` as a client component with these specifications:

1. Must have `'use client'` directive at top (required for hooks/window access in App Router)
2. Use `useState` for progress percentage, `useEffect` for scroll event listener
3. Calculate progress as: `(window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100`
4. Render a fixed 3px tall bar at top of viewport, full width
5. Use theme `bg-primary` for the bar color, z-index 50 to stay on top
6. Add smooth CSS transition for width changes: `transition-all duration-100`
7. Add passive event listener for better scroll performance: `{ passive: true }`
8. Clean up event listener on unmount

After component is created, add it to `app/posts/[slug]/page.tsx` inside the `Container` component before the article closing tag. </action> <verify> <automated> grep -q "ReadingProgressBar" components/ReadingProgressBar.tsx && grep -q "ReadingProgressBar" app/posts/[slug]/page.tsx && npm run lint -- --quiet </automated> </verify> <done>

- File `components/ReadingProgressBar.tsx` exists with exported component
- Component has `'use client'` directive
- Progress bar renders fixed at top, 3px height, bg-primary color
- Component imported and rendered in post page
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- components/ReadingProgressBar.tsx contains `'use client'`
- components/ReadingProgressBar.tsx contains `export function ReadingProgressBar`
- components/ReadingProgressBar.tsx contains `addEventListener('scroll'`
- app/posts/[slug]/page.tsx contains `import ReadingProgressBar`
- app/posts/[slug]/page.tsx contains `<ReadingProgressBar`
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 2: Implement Back to Top Button component</name>
  <read_first>
    app/posts/[slug]/page.tsx
    tailwind.config.ts
  </read_first>
  <action>
Create `components/BackToTopButton.tsx` as a client component with these specifications:

1. Must have `'use client'` directive at top
2. Use `useState` to track visibility - only show when scrolled past 400px
3. Use `useEffect` to listen for scroll events to update visibility
4. When clicked, call `window.scrollTo({ top: 0, behavior: 'smooth' })` for native smooth scroll
5. Position: fixed bottom-6 right-6, z-index 40
6. Styling: circular button 48×48px, background with backdrop blur, border, shadow-sm, hover:scale-105 transition-all
7. Use `ArrowUp` icon from `lucide-react`
8. Fade-in/fade-out transition when visibility changes (opacity transition)
9. Accessibility: `aria-label="Back to top"`

After component is created, add it to `app/posts/[slug]/page.tsx` after ReadingProgressBar. </action> <verify> <automated> grep -q "BackToTopButton" components/BackToTopButton.tsx && grep -q "BackToTopButton" app/posts/[slug]/page.tsx && npm run lint -- --quiet </automated> </verify> <done>

- File `components/BackToTopButton.tsx` exists with exported component
- Component has `'use client'` directive
- Button fades in after scrolling past 400px
- Click triggers smooth scroll to top
- Button positioned at bottom-right corner
- Imported and rendered in post page
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- components/BackToTopButton.tsx contains `'use client'`
- components/BackToTopButton.tsx contains `export function BackToTopButton`
- components/BackToTopButton.tsx contains `window.scrollTo`
- components/BackToTopButton.tsx contains `behavior: 'smooth'`
- app/posts/[slug]/page.tsx contains `import BackToTopButton`
- app/posts/[slug]/page.tsx contains `<BackToTopButton`
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 3: Implement Copy Button component for code blocks</name>
  <read_first>
    contentlayer.config.ts
  </read_first>
  <action>
Create `components/CopyButton.tsx` as a client component with these specifications:

1. Must have `'use client'` directive at top
2. Props: `{ text: string }` - the text to copy to clipboard
3. Use `useState` to track copied state (false by default)
4. On click: use `navigator.clipboard.writeText(text)` to copy, set copied to true for 2 seconds then revert
5. Render: absolute positioned button at top-2 right-2, semi-transparent background, backdrop-blur-sm, border, hover:bg-accent
6. Use `Copy` icon from `lucide-react` when not copied, `Check` icon (green color) when copied
7. Size 16px icon, p-2 padding, rounded-md
8. Accessibility: `aria-label="Copy code"`

Notes: This component will be used by a wrapper around code blocks. For now just create the reusable component with correct API. </action> <verify> <automated> grep -q "CopyButton" components/CopyButton.tsx && grep -q "navigator.clipboard" components/CopyButton.tsx && npm run lint -- --quiet </automated> </verify> <done>

- File `components/CopyButton.tsx` exists with exported component
- Component accepts `text: string` prop
- Click copies text to clipboard using Clipboard API
- Visual feedback: icon changes to checkmark when copied
- Correct positioning for overlay on code blocks
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- components/CopyButton.tsx contains `'use client'`
- components/CopyButton.tsx contains `export function CopyButton`
- components/CopyButton.tsx contains `navigator.clipboard.writeText`
- components/CopyButton.tsx contains `type CopyButtonProps = { text: string }`
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

</tasks>

<verification>
All three components must render without hydration errors. Check that:
1. No `window is not defined` errors at build time
2. Components don't cause content layout shift
3. Dark/light theme styling works correctly
</verification>

<success_criteria>

- ReadingProgressBar component renders at top of post page and updates width on scroll
- BackToTopButton appears after scrolling past threshold and smooth scrolls to top
- CopyButton component exists with correct props and clipboard integration
- All components are client components with `'use client'` directive
- No lint errors, no hydration errors </success_criteria>

<output>
After completion, create `.planning/phases/05-enhanced-ux/05-01-SUMMARY.md` with implementation summary.
</output>
