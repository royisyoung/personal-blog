---
phase: 05-enhanced-ux
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - lib/generate-rss.ts
  - scripts/generate-rss.js
  - package.json (build script)
  - app/components/Footer.tsx
  - public/.gitkeep
  - app/layout.tsx
autonomous: true
requirements: [UX-06]
user_setup: []

must_haves:
  truths:
    - 'RSS feed generated automatically at build time to public/rss.xml'
    - 'RSS feed includes full article content and valid RSS 2.0 format'
    - 'RSS link available in footer for subscription'
  artifacts:
    - path: 'lib/generate-rss.ts'
      provides: 'RSS generation function using rss package'
      exports: ['generateRSS']
    - path: 'scripts/generate-rss.js'
      provides: 'CLI script for build-time execution'
    - path: 'package.json'
      provides: 'RSS generation added to build script'
    - path: 'app/components/Footer.tsx'
      provides: 'Footer with RSS link added'
  key_links:
    - from: 'npm build'
      to: 'scripts/generate-rss.js'
      via: 'postbuild script runs RSS generation'
    - from: 'RSS generation'
      to: 'public/rss.xml'
      via: 'writes XML file after build'
    - from: 'Footer'
      to: '/rss.xml'
      via: 'link for readers to subscribe'
---

<objective>
实现静态构建时 RSS 订阅源生成，并添加 RSS 链接到 footer。

Purpose: 让读者可以通过 RSS 阅读器订阅博客，自动获取新文章通知。静态生成符合 SSG 架构，零运行时开销。

Output: rss 依赖安装，构建脚本集成，XML 输出生成，footer 链接添加。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-enhanced-ux/05-CONTEXT.md
@.planning/phases/05-enhanced-ux/05-RESEARCH.md
@lib/content.ts
@next.config.ts

<interfaces>
From lib/content.ts:
```typescript
export function getAllPosts(): Post[];
```
From research:
```javascript
const feed = new RSS({ title, site_url, feed_url });
posts.forEach(post => feed.item({ ... }));
fs.writeFileSync('./public/rss.xml', feed.xml({ indent: true }));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install rss dependency and create RSS generation script</name>
  <files>package.json, pnpm-lock.yaml, lib/generate-rss.ts, scripts/generate-rss.js, public/.gitkeep</files>
  <read_first>
    package.json
    lib/content.ts
  </read_first>
  <action>
1. Install `rss` dependency via `pnpm add rss`
2. Install `@types/rss` dev dependency via `pnpm add -D @types/rss`
3. Create `lib/generate-rss.ts` with:
   - Import RSS from 'rss'
   - Import { getAllPosts } from '@/lib/content'
   - Import type { Post } from 'contentlayer/generated'
   - Export async function `generateRSS()` that:
     - Gets site URL from `process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'`
     - Creates new RSS feed with site title, description, site URL, feed URL
     - Iterates all posts sorted by date descending, adds each as item:
       - title, description (post.description), url, date, full content
       - Include full content in RSS per user decision
     - Writes generated XML to `public/rss.xml` with 2-space indentation
4. Create `scripts/generate-rss.js` CLI entry that:
   - Runs `generateRSS()` function, handles errors, exits with proper code
5. Update `package.json` build script: add `&& node scripts/generate-rss.js` to run after build completes
  </action>
  <verify>
    <automated>
grep -q "rss" package.json && test -f lib/generate-rss.ts && test -f scripts/generate-rss.js && npm run lint -- --quiet
    </automated>
  </verify>
  <done>
- rss and @types/rss installed
- generateRSS function exported from lib/generate-rss.ts
- Script uses getAllPosts to include all posts in feed
- Includes full article content in RSS per decision
- Writes output to public/rss.xml
- CLI script in scripts/generate-rss.js
- Build script updated to run RSS generation after build
- `npm run lint` passes with no errors
  </done>
  <acceptance_criteria>
- package.json contains `"rss"` in dependencies
- package.json contains `"@types/rss"` in devDependencies
- lib/generate-rss.ts contains `export async function generateRSS`
- lib/generate-rss.ts contains `new RSS`
- lib/generate-rss.ts contains `writeFileSync.*public/rss.xml`
- scripts/generate-rss.js exists and imports generateRSS
- package.json build script contains `generate-rss`
- `npm run lint -- --quiet` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add RSS link to site footer</name>
  <files>app/components/Footer.tsx</files>
  <read_first>
    components/Header.tsx
    app/page.tsx
  </read_first>
  <action>
Check existing footer location. If footer doesn't exist, create `app/components/Footer.tsx` server component:

1. Create footer with site copyright information
2. Add navigation links that match current site navigation
3. Add RSS subscription link to `/rss.xml` with RSS icon from lucide-react
4. Add rel="alternate" type="application/rss+xml" attributes for RSS auto-discovery
5. Style footer: subtle, centered text, padding 2rem 0, text-muted-foreground
6. Add Footer to bottom of page on all pages by adding it to the main Container layout

Check existing Container component: If Container already in layout, add Footer inside Container at the end. </action> <verify> <automated> grep -q "rss.xml" app/components/Footer.tsx && npm run lint -- --quiet </automated> </verify> <done>

- Footer component created with copyright and links
- RSS link present with correct href="/rss.xml"
- RSS icon included from lucide-react
- Proper RSS auto-discovery attributes
- Footer rendered at bottom of all pages
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- app/components/Footer.tsx contains `href="/rss.xml"`
- app/components/Footer.tsx contains `type="application/rss+xml"`
- app/components/Footer.tsx contains `RSS`
- Container layout includes Footer component
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 3: Add RSS auto-discovery link to root layout</name>
  <files>app/layout.tsx</files>
  <read_first>
    app/layout.tsx
  </read_first>
  <action>
1. Open `app/layout.tsx` where metadata is defined
2. Add RSS auto-discovery link to metadata:
```
links: [
  { rel: 'alternate', type: 'application/rss+xml', href: '/rss.xml', title: 'RSS feed' },
],
```
This allows browsers and RSS readers to auto-discover the RSS feed.
  </action>
  <verify>
    <automated>
grep -q "application/rss+xml" app/layout.tsx && npm run lint -- --quiet
    </automated>
  </verify>
  <done>
- RSS alternate link added to layout metadata
- Correct type and href attributes
- Auto-discovery works for readers/browsers
- `npm run lint` passes with no errors
  </done>
  <acceptance_criteria>
- app/layout.tsx contains `application/rss+xml`
- app/layout.tsx contains `href="/rss.xml"`
- `npm run lint -- --quiet` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After running build, check that `public/rss.xml` exists and contains valid RSS 2.0 XML with all posts. Validate XML syntax.
</verification>

<success_criteria>

- rss package installed and types configured
- RSS generation runs automatically after build completes
- RSS feed generated as public/rss.xml with all posts
- RSS feed includes full article content per requirement
- RSS link present in footer
- RSS auto-discovery link in root layout
- Build completes successfully with RSS generation </success_criteria>

<output>
After completion, create `.planning/phases/05-enhanced-ux/05-05-SUMMARY.md` with implementation summary.
</output>
