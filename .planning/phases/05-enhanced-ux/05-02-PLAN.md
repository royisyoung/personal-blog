---
phase: 05-enhanced-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vitest.config.ts
  - tests/setup.ts
  - lib/reading-time.ts
  - lib/content.ts
  - contentlayer.config.ts
  - lib/related-posts.ts
autonomous: true
requirements: [UX-05, UX-07]
user_setup: []

must_haves:
  truths:
    - 'Estimated reading time displays below article title, calculated at 200 WPM excluding code blocks'
    - 'Related posts are calculated by shared tag count and displayed at end of article'
    - 'Vitest testing framework is configured and ready for all tests'
  artifacts:
    - path: 'vitest.config.ts'
      provides: 'Vitest configuration for Next.js testing'
    - path: 'lib/reading-time.ts'
      provides: 'Reading time calculation function (excludes code blocks)'
      exports: ['calculateReadingTime']
    - path: 'lib/related-posts.ts'
      provides: 'Related posts calculation by shared tags'
      exports: ['getRelatedPosts']
    - path: 'contentlayer.config.ts'
      provides: 'Adds readingTime computed field to Post'
      contains: 'readingTime'
  key_links:
    - from: 'contentlayer.config.ts'
      to: 'lib/reading-time.ts'
      via: 'computed field resolver calls calculateReadingTime'
    - from: 'post page'
      to: 'Post type'
      via: 'post.readingTime is available for display'
    - from: 'lib/related-posts.ts'
      to: 'contentlayer/generated'
      via: 'works with Post type from Contentlayer'
---

<objective>
搭建测试基础设施，并实现两个需要内容层计算的功能：预计阅读时间计算和相关文章推荐。

Purpose: 预计阅读时间帮助读者判断文章长度，相关文章帮助读者发现更多相关内容。测试基础设施让后续功能可以自动化验证。

Output: Vitest 配置完成，阅读时间计算集成到 Contentlayer，相关文章算法就绪。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-enhanced-ux/05-CONTEXT.md
@.planning/phases/05-enhanced-ux/05-RESEARCH.md
@contentlayer.config.ts
@lib/content.ts

<interfaces>
From contentlayer.config.ts:
```typescript
export const Post = defineDocumentType(() => ({
  name: 'Post',
  fields: { ... },
  computedFields: {
    slug: ...
    // add readingTime here
  },
}));
```

From lib/content.ts:

```typescript
export function getAllPosts(): Post[] { ... }
export function getPostBySlug(slug: string): Post | undefined { ... }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Vitest testing infrastructure</name>
  <read_first>
    package.json
  </read_first>
  <action>
1. Install dev dependencies via `pnpm add -D vitest @testing-library/react @testing-library/jest-dom jsdom`
2. Create `vitest.config.ts` with:
   - Import { defineConfig } from 'vitest/config'
   - Import react from '@vitejs/plugin-react'
   - Use react plugin, test environment set to jsdom
   - Include setup file: `setupFiles: ['./tests/setup.ts']`
3. Create `tests/setup.ts` with:
   - Import '@testing-library/jest-dom/vitest'
4. Verify installation with `npx vitest run --passWithNoTests`
  </action>
  <verify>
    <automated>
test -f vitest.config.ts && test -f tests/setup.ts && grep -q "vitest" package.json && npx vitest run --passWithNoTests
    </automated>
  </verify>
  <done>
- All dev dependencies installed: vitest, @testing-library/react, @testing-library/jest-dom, jsdom
- vitest.config.ts exists with correct configuration
- tests/setup.ts exists and imports jest-dom
- `npx vitest run --passWithNoTests` exits with 0
  </done>
  <acceptance_criteria>
- package.json contains `"vitest"` in devDependencies
- package.json contains `"@testing-library/react"` in devDependencies
- package.json contains `"@testing-library/jest-dom"` in devDependencies
- vitest.config.ts contains `defineConfig`
- vitest.config.ts contains `environment: 'jsdom'`
- tests/setup.ts contains `@testing-library/jest-dom`
- `npx vitest run --passWithNoTests` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement estimated reading time calculation</name>
  <read_first>
    contentlayer.config.ts
    lib/content.ts
  </read_first>
  <action>
1. Create `lib/reading-time.ts` with:
   - Export function `calculateReadingTime(doc: { body: { raw: string } }): number`
   - Algorithm:
     - Extract raw markdown: `doc.body.raw`
     - Remove all code blocks: replace `/```[\\s\\S]*?```/g` with empty string
     - Split by whitespace to count words: `textOnly.trim().split(/\\s+/).length`
     - Calculate minutes: `Math.ceil(words / 200)` (200 WPM per decision)
     - Return `Math.max(minutes, 1)` (minimum 1 minute)
   - Add JSDoc comments explaining the code block exclusion and 200 WPM

2. Add `readingTime` computed field to `contentlayer.config.ts` Post document:
   - In `computedFields` add:

   ```
   readingTime: {
     type: 'number',
     resolve: calculateReadingTime,
   }
   ```

   - Import `calculateReadingTime` from `@/lib/reading-time`

3. In `app/posts/[slug]/page.tsx`, display reading time in the header metadata area:
   - Add after category: `<span>• {post.readingTime} min read</span>` </action> <verify> <automated> grep -q "calculateReadingTime" lib/reading-time.ts && grep -q "readingTime" contentlayer.config.ts && grep -q "min read" app/posts/[slug]/page.tsx && npm run lint -- --quiet </automated> </verify> <done>

- calculateReadingTime function exported from lib/reading-time.ts
- Function excludes code blocks from word count using regex
- Calculation uses 200 WPM, rounds up, minimum 1 minute
- readingTime added as computed field to Post in contentlayer.config.ts
- Reading time displayed in post page header below title
- `npm run lint` passes with no errors </done> <acceptance_criteria>
- lib/reading-time.ts contains `export function calculateReadingTime`
- lib/reading-time.ts contains `/```[\\s\\S]*?```/g`
- lib/reading-time.ts contains `words / 200`
- contentlayer.config.ts contains `readingTime` in computedFields
- app/posts/[slug]/page.tsx contains `min read`
- `npm run lint -- --quiet` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 3: Implement related posts calculation by shared tags</name>
  <read_first>
    lib/content.ts
    contentlayer.config.ts
  </read_first>
  <action>
1. Create `lib/related-posts.ts` with:
   - Import type `{ Post } from 'contentlayer/generated'`
   - Export function `getRelatedPosts(currentPost: Post, allPosts: Post[], limit?: number): Post[]`
   - Algorithm (per research):
     - If currentPost has no tags: return most recent posts (limit = 3 by default)
     - Otherwise for each other post: count number of shared tags with currentPost
     - Sort posts by shared tag count descending, then by date descending (more recent first)
     - Take top N (limit = 3 by default)
     - If not enough posts with shared tags, pad with most recent posts that aren't already included

Algorithm reference from research:

```typescript
// Pseudo-code:
const sharedTags = post.tags?.filter(tag => currentPost.tags.includes(tag)).length || 0;
sort by (b.sharedTags - a.sharedTags), then date desc
```

  </action>
  <verify>
    <automated>
grep -q "getRelatedPosts" lib/related-posts.ts && npm run lint -- --quiet
    </automated>
  </verify>
  <done>
- getRelatedPosts function exported with correct type signature
- Sorting works by number of shared tags (primary) then by recency (secondary)
- Handles edge case when current post has no tags
- Handles case when fewer than limit posts have shared tags
- `npm run lint` passes with no errors
  </done>
  <acceptance_criteria>
- lib/related-posts.ts contains `export function getRelatedPosts`
- lib/related-posts.ts contains `sharedTags` counting
- lib/related-posts.ts contains `filter((post) => post.slug !== currentPost.slug)`
- lib/related-posts.ts contains `sort((a, b) => b.sharedTags - a.sharedTags)`
- `npm run lint -- --quiet` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Contentlayer must regenerate types after adding computed field. Verify that `contentlayer/generated` includes `readingTime` on `Post` type.
</verification>

<success_criteria>

- Vitest infrastructure configured and working
- Reading time correctly calculated excluding code blocks
- Reading time displayed in post header
- Related posts algorithm correctly sorts by shared tags
- No build errors after adding computed field </success_criteria>

<output>
After completion, create `.planning/phases/05-enhanced-ux/05-02-SUMMARY.md` with implementation summary.
</output>
