---
phase: 05-enhanced-ux
plan: 06
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - components/ReadingProgressBar.test.tsx
  - components/BackToTopButton.test.tsx
  - components/CopyButton.test.tsx
  - lib/reading-time.test.ts
  - lib/related-posts.test.ts
  - lib/toc.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-07]
user_setup: []

must_haves:
  truths:
    - 'Every implemented feature has an automated unit test'
    - 'All tests pass (skipped tests are acceptable for pure UI components)'
    - 'Test coverage for all pure utility functions (reading time, related posts, TOC)'
  artifacts:
    - path: 'components/*.test.tsx'
      provides: 'Unit tests for client components'
    - path: 'lib/*.test.ts'
      provides: 'Unit tests for utility functions'
  key_links:
    - from: 'vitest run'
      to: 'all test files'
      via: 'runs full test suite'
---

<objective>
为所有已实现的功能添加单元测试，运行完整测试套件验证一切正常工作。

Purpose: 确保算法逻辑正确，防止回归，为未来重构提供安全保障。测试所有纯函数（计算逻辑），组件测试验证渲染正确性。

Output: 所有测试文件创建完成，完整测试套件运行通过。 </objective>

<execution_context> @/Users/xuyang/.claude/get-shit-done/workflows/execute-plan.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-enhanced-ux/05-CONTEXT.md
@.planning/phases/05-enhanced-ux/05-VALIDATION.md
@vitest.config.ts
@tests/setup.ts
@lib/reading-time.ts
@lib/related-posts.ts
@lib/toc.ts

<interfaces>
From vitest.config.ts:
```typescript
export default defineConfig({
  test: { environment: 'jsdom', setupFiles: ['./tests/setup.ts'] },
  plugins: [react()],
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for utility functions</name>
  <files>lib/reading-time.test.ts, lib/related-posts.test.ts, lib/toc.test.ts</files>
  <read_first>
    lib/reading-time.ts
    lib/related-posts.ts
    lib/toc.ts
    vitest.config.ts
  </read_first>
  <action>
1. Create `lib/reading-time.test.ts`:
   - Test: empty document (but result min 1 minute)
   - Test: short text → 1 minute
   - Test: long text → correct calculation
   - Test: code blocks excluded from word count (verify words in code not counted)
   - Import and test calculateReadingTime function

2. Create `lib/related-posts.test.ts`:
   - Test: current post with no tags returns most recent posts
   - Test: posts with more shared tags come first
   - Test: excludes current post from results
   - Test: returns exactly limit posts (default 3)
   - Import and test getRelatedPosts function

3. Create `lib/toc.test.ts`:
   - Test: slugifyHeading converts text to url-safe slug
   - Test: slugifyHeading handles collisions with unique suffix
   - Test: extractHeadings only extracts h2 and h3 (skips h1, h4+)
   - Test: extracts correct value and generates correct ids </action> <verify> <automated> test -f lib/reading-time.test.ts && test -f lib/related-posts.test.ts && test -f lib/toc.test.ts && npx vitest run lib/ --passWithNoTests </automated> </verify> <done>

- Test files created for all three utility modules
- All main behaviors tested
- All tests pass (or skipped if setup issue)
- npx vitest run lib/ completes without errors </done> <acceptance_criteria>
- lib/reading-time.test.ts exists and contains `describe(` and `test(`
- lib/related-posts.test.ts exists and contains `describe(` and `test(`
- lib/toc.test.ts exists and contains `describe(` and `test(`
- lib/reading-time.test.ts contains `calculateReadingTime`
- lib/related-posts.test.ts contains `getRelatedPosts`
- lib/toc.test.ts contains `slugifyHeading`
- `npx vitest run lib/ --passWithNoTests` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 2: Add unit tests for components</name>
  <files>components/ReadingProgressBar.test.tsx, components/BackToTopButton.test.tsx, components/CopyButton.test.tsx</files>
  <read_first>
    components/ReadingProgressBar.tsx
    components/BackToTopButton.tsx
    components/CopyButton.tsx
    vitest.config.ts
  </read_first>
  <action>
1. Create `components/ReadingProgressBar.test.tsx`:
   - Test: renders without crashing
   - Test: has correct className for positioning
   - Uses render from @testing-library/react

2. Create `components/BackToTopButton.test.tsx`:
   - Test: renders without crashing
   - Test: button has correct aria-label
   - Uses render from @testing-library/react

3. Create `components/CopyButton.test.tsx`:
   - Test: renders without crashing
   - Test: has correct aria-label
   - Uses render from @testing-library/react

Note: Full interaction testing (scroll, clipboard) requires browser, unit test only verifies rendering. </action> <verify> <automated> test -f components/ReadingProgressBar.test.tsx && test -f components/BackToTopButton.test.tsx && test -f components/CopyButton.test.tsx && npx vitest run components/ --passWithNoTests </automated> </verify> <done>

- Test files created for all three components
- Components render without crashing
- Accessibility attributes verified
- All tests pass
- npx vitest run components/ completes without errors </done> <acceptance_criteria>
- components/ReadingProgressBar.test.tsx exists and contains `render(`
- components/BackToTopButton.test.tsx exists and contains `render(`
- components/CopyButton.test.tsx exists and contains `render(`
- `npx vitest run components/ --passWithNoTests` exits with code 0 </acceptance_criteria> </task>

<task type="auto">
  <name>Task 3: Run full test suite and lint check</name>
  <files>package.json</files>
  <read_first>
  </read_first>
  <action>
1. Run `npm run lint` to check all new code for errors
2. Run `npx vitest run --passWithNoTests` to run full test suite
3. Fix any lint errors or failing tests before completing
  </action>
  <verify>
    <automated>
npm run lint -- --quiet && npx vitest run --passWithNoTests
    </automated>
  </verify>
  <done>
- All lint checks pass
- All tests pass (skipped tests allowed)
- Full test suite completes successfully
- No type errors in new code
  </done>
  <acceptance_criteria>
- `npm run lint -- --quiet` exits with code 0
- `npx vitest run --passWithNoTests` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
All tests must pass. If some tests fail because of mocking issues (window/clipboard), they can be skipped - that's acceptable. The goal is to test the logic, not browser APIs.
</verification>

<success_criteria>

- Every utility function (reading-time, related-posts, toc) has unit tests covering main behaviors
- Every client component has at least a smoke test (renders without crashing)
- All lint checks pass
- Full test suite run completes successfully
- No TypeScript type errors </success_criteria>

<output>
After completion, create `.planning/phases/05-enhanced-ux/05-06-SUMMARY.md` with test results summary.
</output>
